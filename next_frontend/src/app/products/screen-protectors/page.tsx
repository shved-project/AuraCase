import {Metadata} from "next";

export const metadata: Metadata = {
    title: "Защитные стёкла",
};

const ScreenProtectors = () => {
    return <h1>ScreenProtectors</h1>;
};

export default ScreenProtectors;
